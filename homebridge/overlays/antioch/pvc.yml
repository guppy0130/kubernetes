---
apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: "homebridge-pvc"
spec:
  resources:
    requests:
      storage: "256Mi"
  volumeMode: "Filesystem"
  accessModes:
    - "ReadWriteOnce"
  storageClassName: "ceph-filesystem"
